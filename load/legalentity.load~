LOAD CSV WITH HEADERS FROM 'https://raw.githubusercontent.com/AcuoFS/acuo-data/master/CSV/entity.csv' 
AS line

MATCH (c:Client {id:line.ClientId})

MERGE (e:LegalEntity {id:line.EntityId, name:line.EntityName})

MERGE (c)-[:MANAGES]->(e);
