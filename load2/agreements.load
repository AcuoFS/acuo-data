LOAD CSV WITH HEADERS FROM 'https://raw.githubusercontent.com/AcuoFS/acuo-data/master/CSV2/agreements.csv' 
AS line

MERGE (:Agreement {Id:line.Id, Name:line.Name, Date:line.Date, Type:line.Type})

WITH line

MATCH (a:Agreement {id:line.Id})
WHERE line.CCP IS NOT NULL 
SET a.CCP = line.CCP