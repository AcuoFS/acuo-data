LOAD CSV WITH HEADERS FROM '%dataImportLink%/csv/currencies.csv'
AS line

MERGE (c1:Currency {id:line.CurrencyISO, name:line.CurrencyName})

WITH *

MATCH (c2:Currency {id:'USD'})

MERGE (c1)-[:FX_RATE {fxRate:toFloat(line.ValueToUSD)}]->(c2)

